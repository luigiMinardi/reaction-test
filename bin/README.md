# Binary Directory

This is not the releases directory but a directory for locally generated
binaries through `make run`. Those are not commited into git so this directory
should always be empty. (aside from this README)
